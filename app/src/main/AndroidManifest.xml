<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookSwap"
        tools:targetApi="31">

        <!-- THIS IS THE FIX: Added the declaration for PersonalDetailsActivity -->
        <activity
            android:name=".PersonalDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ProfileActivity"
            android:theme="@style/Theme.BookSwap" />

        <activity
            android:name=".AllBooksActivity"
            android:exported="false" />
        <activity
            android:name=".CartActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".BooksForSaleActivity"
            android:exported="false"
            android:parentActivityName=".ProfileActivity"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".PasswordChangeActivity"
            android:exported="false"
            android:parentActivityName=".ProfileActivity"
            android:theme="@style/Theme.BookSwap"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".OrderTrackingActivity"
            android:exported="false"
            android:parentActivityName=".ProfileActivity"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".AddBookActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.BookSwap"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".BookDetailActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.BookSwap" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BookSwap.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/Theme.BookSwap.NoActionBar" />
        <activity
            android:name=".InsightActivity"
            android:theme="@style/Theme.BookSwap.NoActionBar" />
        <activity
            android:name=".StartPageActivity"
            android:theme="@style/Theme.BookSwap.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.BookSwap.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/Theme.BookSwap.NoActionBar"
            android:windowSoftInputMode="adjustResize" /> <!-- File Provider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>

